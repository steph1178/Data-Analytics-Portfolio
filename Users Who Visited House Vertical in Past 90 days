SELECT * 
FROM national_subscribers a 
LEFT JOIN
  (
  SELECT user_id
  FROM pages
  WHERE context_page_path LIKE
  '/home%' 
  AND timestamp >= CURRENT_DATE - 90 
  GROUP BY 1
  )b
ON a.visitor_id = b.user_id 
WHERE b.user_id IS NOT NULL
